package meta.SimulatorInterface;

public interface LayerInput {
    /*
    method translated data format
     */
    void receiveDataStream();

    /*
        today's goal finished tuple and tuple iterator
        transform tuple into node,

     */
    /*
    * receive data stream tuple x and iterate tuple until done
    *
    * for meta all tuple should become nodes for adding to the local map and goals
    *
    *
    * */
}
